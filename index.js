var name = 'Arwa';
var age = 20;
var isStudent = true;  // arwa(me) assumed this is a true boolean.
console.log('name', name);
console.log('age', age);
console.log('isStudent', isStudent);

var variableToCheck = 100; // arwa(me) declared this variable to be a number.
console.log('the data type of the variable "variableToCheck" is ', typeof variableToCheck);

var greeting = 'Welcome';
// string name = 'arwa'; //since the var name is alrady declared, there is no need to declare it again.
console.log(greeting + ' ' + name) //when checked the name and greeting were stuch to each other therefore i added a space between them.
